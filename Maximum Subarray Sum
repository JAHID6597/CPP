#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int arr[n],sum=0,ans=0;
    for(int i=0;i<n;i++){
        cin>>arr[i];
        sum = max(arr[i],sum+arr[i]);
        ans = max(ans,sum);
    }
    cout<<ans<<endl;
    return 0;
}
